# Changelog

## 2026-09-07

- Added causal defect probes for attention, grouping, decision costs, action scope, state contradictions, and visual craft.
- Added counterarguments, observable acceptance conditions, and before/after judgments that expose regressions.
- Kept strict evidence formats for structured handoffs while removing mandatory dossiers from ordinary improvements.
- Made native scrollbar findings contextual in both guidance and detector messages.
- Added a dispatch fixture and a forward-test case for hidden selection, false success, hierarchy, and preservation.
- Validation: package/evaluation validators, quick_validate, and 91 targeted Node tests passed. A paired forward audit on one fixture detected the same two task failures in both versions; it does not measure cross-model improvement.
- Forward implementation: an isolated copy passed eight checks at 1440 x 900 for hierarchy, visible selection scope, simulated failure and retry, pending state, keyboard focus, and preservation of data and native controls. Rendered comparison confirmed that all six rows and action feedback fit before the secondary summary. This validates one local fixture, not production use.

## 2026-08-12

- Migrated package scripts and lockfile from npm to pnpm 11.20.0.
- Updated Playwright to 1.62.1 and recorded the upstream release-notes location.

